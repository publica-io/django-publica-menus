============
Installation
============

At the command line::

    $ easy_install django-publica-menus

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-menus
    $ pip install django-publica-menus
